import { StyleSheet, Text, View } from "react-native";
import React from "react";

const cart = () => {
  return <View></View>;
};

export default cart;

const styles = StyleSheet.create({});
